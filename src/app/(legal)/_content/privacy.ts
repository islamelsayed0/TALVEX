import type { LegalDocument } from '@/lib/legal/documents'

/**
 * Privacy Policy.
 *
 * AWAITING COPY, on the same terms as _content/terms.ts. Paste the drafted
 * markdown into `source` verbatim, set `effective`, delete this paragraph.
 * Everything around it is already built.
 */
export const PRIVACY: LegalDocument = {
  title: 'Privacy Policy',
  description:
    'What data Talvex collects, why we hold it, and the choices you have over it.',
  source: '',
}
